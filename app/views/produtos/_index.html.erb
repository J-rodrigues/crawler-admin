
<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Preço</th>
            <th>Url</th>
        </tr>
    </thead>
    <tbody>
        <% @produtos = Produto.all.page(params[:page]).order("id").per(3) %>
        <% @produtos.each do |produto| %>
        <tr>
          <td><%= produto.nome %></td>
          <td><%= produto.preco%></td>
          <td><%= produto.url  %></td>
        </tr>
      <% end %>
    </tbody>
</table>



